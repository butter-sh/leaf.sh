<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{project_name}} - {{project_description}}</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      background: #f9fafb;
    }
    header {
      background: linear-gradient(135deg, #22c55e 0%, #4ade80 100%);
      color: white;
      padding: 4rem 2rem;
      text-align: center;
    }
    header h1 { font-size: 3rem; margin-bottom: 1rem; }
    header p { font-size: 1.5rem; opacity: 0.9; }
    main { max-width: 1200px; margin: 3rem auto; padding: 0 2rem; }
    .projects-title {
      text-align: center;
      font-size: 2.5rem;
      margin-bottom: 3rem;
      color: #22c55e;
    }
    .projects {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }
    .project {
      background: white;
      border-radius: 12px;
      padding: 2rem;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .project:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 12px rgba(0,0,0,0.15);
    }
    .project h2 {
      color: #22c55e;
      font-size: 1.8rem;
      margin-bottom: 0.5rem;
    }
    .project .tagline {
      color: #666;
      font-size: 0.9rem;
      margin-bottom: 1rem;
    }
    .project .description {
      color: #555;
      margin-bottom: 1.5rem;
    }
    .project a {
      color: #22c55e;
      text-decoration: none;
      font-weight: 600;
    }
    .project a:hover { text-decoration: underline; }
    footer {
      text-align: center;
      padding: 3rem 2rem;
      color: #666;
      border-top: 1px solid #e5e7eb;
      margin-top: 4rem;
    }
    footer a { color: #22c55e; text-decoration: none; }
    footer a:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <header>
    <h1>{{project_name}}</h1>
    <p>{{project_description}}</p>
  </header>

  <main>
    <h2 class="projects-title">Projects</h2>
    <div class="projects">
      {{{projects_html}}}
    </div>
  </main>

  <footer>
    <p>Part of the <a href="{{github_org}}">{{project_name}}</a> ecosystem</p>
    <p style="margin-top: 0.5rem;"><em>Unlimited. Independent. Fresh.</em></p>
  </footer>
</body>
</html>
