<!DOCTYPE html>
<html lang="en" class="scroll-smooth dark">
{{> _head}}
{{> _carbon_styles}}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/atom-one-dark.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/languages/bash.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

<style>
    /* Enhanced prose styles for README with carbon theme */
    .readme-prose {
        font-size: 1.0625rem;
        line-height: 1.75;
        color: #e5e7eb;
    }
    .readme-prose h1 {
        font-size: 2.25rem;
        font-weight: 800;
        line-height: 1.2;
        margin-top: 0;
        margin-bottom: 1.5rem;
        color: var(--carbon-light);
    }
    .readme-prose h2 {
        font-size: 1.875rem;
        font-weight: 700;
        line-height: 1.3;
        margin-top: 2.5rem;
        margin-bottom: 1.25rem;
        color: var(--carbon-light);
        border-bottom: 2px solid rgba(134, 239, 172, 0.3);
        padding-bottom: 0.5rem;
    }
    .readme-prose h3 {
        font-size: 1.5rem;
        font-weight: 600;
        line-height: 1.4;
        margin-top: 2rem;
        margin-bottom: 1rem;
        color: var(--carbon-light-dim);
    }
    .readme-prose p {
        margin-bottom: 1.25rem;
        color: #e5e7eb;
    }
    .readme-prose a {
        color: var(--carbon-light);
        text-decoration: underline;
        text-decoration-color: rgba(134, 239, 172, 0.5);
        transition: all 0.2s;
        font-weight: 500;
    }
    .readme-prose a:hover {
        color: var(--carbon-light-dim);
        text-decoration-color: var(--carbon-light);
    }
    .readme-prose code {
        background: rgba(134, 239, 172, 0.1);
        color: var(--carbon-light);
        padding: 0.125rem 0.375rem;
        border-radius: 0.25rem;
        font-size: 0.9em;
        font-weight: 600;
        border: 1px solid rgba(134, 239, 172, 0.2);
    }
    .readme-prose pre {
        background: #1e293b !important;
        border-radius: 0.5rem;
        padding: 1.25rem;
        overflow-x: auto;
        margin: 1.5rem 0;
        border: 1px solid rgba(134, 239, 172, 0.2);
    }
    .readme-prose pre code {
        background: transparent;
        color: inherit;
        padding: 0;
        font-size: 0.875rem;
        font-weight: 400;
        border: none;
    }
    .readme-prose ul, .readme-prose ol {
        margin: 1.25rem 0;
        padding-left: 1.75rem;
        color: #e5e7eb;
    }
    .readme-prose li {
        margin: 0.5rem 0;
        color: #e5e7eb;
    }
    .readme-prose blockquote {
        border-left: 4px solid var(--carbon-light);
        padding-left: 1.25rem;
        margin: 1.5rem 0;
        color: #cbd5e1;
        font-style: italic;
    }
    .readme-prose strong {
        font-weight: 700;
        color: var(--carbon-light);
    }
    .readme-prose table {
        width: 100%;
        border-collapse: collapse;
        margin: 1.5rem 0;
    }
    .readme-prose th, .readme-prose td {
        border: 1px solid rgba(134, 239, 172, 0.2);
        padding: 0.75rem;
        text-align: left;
        color: #e5e7eb;
    }
    .readme-prose th {
        background: rgba(134, 239, 172, 0.1);
        font-weight: 600;
        color: var(--carbon-light);
    }
    
    /* Collapsible source file styles */
    .source-file {
        transition: all 0.3s ease;
    }
    .source-file-header {
        cursor: pointer;
        user-select: none;
        transition: all 0.2s;
        background: rgba(134, 239, 172, 0.15) !important;
    }
    .source-file-header:hover {
        background: rgba(134, 239, 172, 0.25) !important;
        box-shadow: 0 0 20px rgba(134, 239, 172, 0.2);
    }
    .source-file-content {
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.3s ease-out;
    }
    .source-file.expanded .source-file-content {
        max-height: 10000px;
        transition: max-height 0.5s ease-in;
    }
    .chevron {
        transition: transform 0.3s ease;
    }
    .source-file.expanded .chevron {
        transform: rotate(180deg);
    }
</style>
</head>
<body class="carbon-bg min-h-screen text-slate-100">
    {{> _header}}

    <!-- Hero Section -->
    <section class="py-20 px-4">
        <div class="max-w-7xl mx-auto text-center">
            <div class="inline-block mb-8 animate-fade-in">
                <div class="w-24 h-24 mx-auto">{{{icon}}}</div>
            </div>
            <h1 class="text-6xl font-bold mb-6 animate-fade-in" style="color: var(--carbon-light);">
                {{project_name}}
            </h1>
            <p class="text-2xl text-slate-300 mb-8 max-w-3xl mx-auto animate-fade-in">
                {{project_description}}
            </p>
            <span class="px-3 py-1 text-sm font-semibold rounded-full animate-fade-in inline-block" 
                  style="background: rgba(134, 239, 172, 0.2); color: var(--carbon-light);">
                v{{project_version}}
            </span>
        </div>
    </section>

    <!-- Overview Section -->
    <section id="overview" class="py-16 px-4">
        <div class="max-w-5xl mx-auto">
            <div class="card-project p-8 md:p-12 rounded-2xl">
                <h2 class="text-4xl font-bold mb-8" style="color: var(--carbon-light);">Overview</h2>
                <div id="readmeContent" class="readme-prose"></div>
            </div>
        </div>
    </section>

    <!-- Source Files Section -->
    <section id="source" class="py-16 px-4">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-bold mb-12 text-center" style="color: var(--carbon-light);">
                Source Files
            </h2>
            <div class="grid gap-6">{{{source_files_html}}}</div>
        </div>
    </section>

    <!-- Examples Section -->
{{#if has_examples}}
    <section id="examples" class="py-16 px-4">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-4xl font-bold mb-12 text-center" style="color: var(--carbon-light);">
                Examples
            </h2>
            <div class="grid gap-6">{{{examples_html}}}</div>
        </div>
    </section>
{{/if}}

    {{> _footer}}

    <!-- Store README content as JSON data -->
    <script id="readmeData" type="application/json">{{{readme_content_json}}}</script>
    
    {{> _docs_scripts}}
</body>
</html>
